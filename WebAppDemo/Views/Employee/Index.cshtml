@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@using WebAppDemo.Entities

@model List<Department>



<div class="row m-auto">
    <div class="col-6">
        <table class="table table-striped table-bordered table-hover">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayName(item.Id.ToString())</td>
                        <td>@Html.DisplayName(item.Name)</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


@*
<div class="container">
    <div class="row">
        <div class="col-6">
            <table class="table table-striped table-bordered table-hover">
                <thead>
                    <tr>
                        <th>
                            FullName
                        </th>
                        <th>
                            Designation
                        </th>
                        <th>
                            DateofJoining
                        </th>
                        <th>
                            EmpId
                        </th>
                        <th>
                            ContactNo
                        </th>
                        <th>
                            Department
                        </th>
                        <th>
                            Location
                        </th>
                        <th>
                            Salary
                        </th>
                    </tr>
                </thead>
                <tbody>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayName(item.FullName)</td>
                            <td>@Html.DisplayName(item.Designation)</td>
                            <td>@Html.DisplayName(item.DateofJoining.ToString("dd-MMM-yy"))</td>
                            <td>@Html.DisplayName(item.EmpID)</td>
                            <td>@Html.DisplayName(item.ContactNo)</td>
                            <td>@Html.DisplayName(item.Department)</td>
                            <td>@Html.DisplayName(item.Location)</td>
                            <td>@Html.DisplayName(item.Salary.ToString())</td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
    </div>

</div>*@